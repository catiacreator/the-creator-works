'use client';

import { Fluxo } from '@/components/criar/fluxo';

/** Criar: tipo → objetivo → formato, e daí para a conversa com a Cát.IA. */
export default function CriarPage() {
  return <Fluxo />;
}
